---
interface Props {
  fullWidth?: boolean;
}

const { fullWidth = true } = Astro.props; // Always full-width by default
---

<section class:list={["content-section", { "full-width": fullWidth }]}>
  <h2>Mots-Clés</h2>
  <div class="keywords">
    <slot />
  </div>
</section>
