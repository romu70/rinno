---
interface Props {
  fullWidth?: boolean;
}

const { fullWidth = false } = Astro.props;
---

<section class:list={["content-section", { "full-width": fullWidth }]}>
  <h2>Contexte</h2>
  <slot />
</section>
